projection implementation in class ZBP_C_PENALTIS_20 unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_PENALTIS_20 alias Penalti
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  side effects
  {
    field DirDisparo affects field Resultado;
    field DirPortero affects field Resultado;
  }
}